<% if @paper.timing!=0 %>
  <script>
  $(document).ready(function(){
  var now=new Date();
  if($('#timeup').html()==0){
    stopTest();
  }
  now.setMinutes(now.getMinutes()+<%=@min%>);
  now.setSeconds(now.getSeconds()-<%=@sec%>);
  $('#time').countdown({until:now, format: 'MS', onExpiry:stopTest});
});

  });

  function stopTest(){
    $('#time').html('<center><h4>Time\'s up!</h4></center>');
    $('#timeup').html('0');
    // $('input.radio').attr("onclick","return false;");
    $("#new_result").submit();

  }

  </script>
<%end%>
<div>
<% if @paper.timing!=0 %><div id="time" class="bg"></div><%end%>
<br>



<%= form_for Result.new do |f| %> 
  <%#= question_form.error_notification %> 
  <%#= f.fields_for :questions do |builder| %>
  <%#= builder.text_field :question %>
    <%#= render "question_fields", :f => builder %>
  <%# end %>

  <div class="inputs"> 
   <%# raise @paper.inspect %>
        <% @paper.questions.each do |question| %> 
         <%= f.hidden_field :userchoice_id, :value => session[:id] %>

         <%#= f.hidden_field :exm_id, :value => session[:exam_id] %> 

         <%#= raise session[:exam_id].inspect %>
         <%= f.hidden_field :exam_id, :value => session[:exam_id] %>


        <%# for question in @paper.questions %>
    <%#= question_form.input :category, :collection => 
get_categories, :include_blank => false %> 
        <%#= f.hidden_field "question_id_#{question.id}", :value => question.id  %>
        <%= question.content %><br>

        <% for answer in question.answers %>
          <%#=  :answer do |choice_form| %> 
          <%#= choice_form :content %> 
          <%#= radio_button_tag("question[answers_attributes][][correct]", 
"false") %> 
        
          <%= radio_button_tag("result[question_id][#{question.id}]", answer.id )  %>
          <%= answer.content %><br>
        <% end %> 
  </div> 
<% end %>
  <div class="actions"> 
    <%= f.button :submit %> 
  </div> 
<% end %>

<div style='display:none' id="timeup"><% if @time==0 %>0<%else%>1<%end%></div>



