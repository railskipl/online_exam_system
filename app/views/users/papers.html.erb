<div class="row">
<div class="span3 scroller affix" style="padding: 4px 4px 4px 4px;">
<% if @paper.timing!=0 %><div id="time" class="bg"></div><%end%>
<br>
 
</div>
<div id="confirmDiv"></div>
<div class="span11 offset3">
      <h4 class="pull-right"><span class="badge badge-important">Questions</span> |<span class="badge badge-success">Answered</span> | No. of Questions: <%= @questions.count%> <% if @paper.timing!=0 %>| Timing: <%=@paper.timing %><%end%></h4><br>

  <%= form_for Result.new ,:id=>'answers-form' do |f| %> 
      <%# @questions.each_with_index do |question , i | %> 
    
<% for question in @questions %>

       <%= f.hidden_field :userchoice_id, :value => session[:id] %>
        <%= f.hidden_field :exam_id, :value => session[:exam_id] %><br>
          <%#= raw('<div id="q_'+i.to_s+'">') %>
            <%= raw('<div id="q_'+question.to_s+'">') %>
 <hr class="double-gradient"/>
  <div class="row">
    <div class="span1">
      <span class="badge badge-important"><h5 class="question-no">Q <%=question.id %></h5></span>
    </div>
        <div class="span 10">
        
        <%=simple_format question.content %>
        </div>
    </div>
    <br /><center>
     <table class="table table-condensed">

     <% question.answers.each_with_index do |ans,index| %>
     <%#raise ans.inspect%>
  <%# for answer in question.answers %>
    <%# session[:selected_answer] = Array.new  %>
   <%#= raise  session[:selected_answer].inspect %>
  <%# session[:selected_answer] << ans.id %>
  <%#= raise  session[:selected_answer].inspect %>
    <tr>
    <td><%=@alpha[index]%>.</td>
    <td><strong><%=simple_format ans.content %></strong></td>
    <%#= raise question.id.inspect %>
   
    <td class="radio pull-right">
    <%= radio_button_tag("result[question_id][#{question.id}]",ans.id) %></td>
    
    </tr>
  <% end %> 
 </table>
 </center>
</div>
<% end %>
 <%= will_paginate @questions, renderer: BootstrapPagination::Rails %>

  <center><button class="btn btn-primary confirmButton" >Submit Answers</button></center>
 <% end %>
 </center>
</div>
<br><br><br>






<div style='display:none' id="timeup"><% if @time==0 %>0<%else%>1<%end%></div>


<!-- Added javascript  for back button-->


    <script>

        jQuery(document).ready(function($) {

          if (window.history && window.history.pushState) {

          window.history.pushState('forward', null, './#forward');

          $(window).on('popstate', function() {
          history.forward();
  
          });

      }
    });
    </script>

<!-- 
Added Timer Javascript in Test
 -->
<% if @paper.timing!=0 %>
        <script>
        $(document).ready(function(){
        var now=new Date();
        if($('#timeup').html()==0){
          stopTest();
        }
        now.setMinutes(now.getMinutes()+<%=@min%>);
        now.setSeconds(now.getSeconds()-<%=@sec%>);
       $('#time').countdown({until:now, format: 'MS',onExpiry:stopTest});

        });

        function stopTest(){
          $('#time').html('<center><h4>Time\'s up!</h4></center>');
          $('#timeup').html('0');
          // $('input.radio').attr("onclick","return false;");
          $("#new_result").submit();
        }
        </script>
<%end%>
 