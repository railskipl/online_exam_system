
<div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <div class="container nav-collapse">

        			<% if adminsection_signed_in? %>
  		<p>Welcome <%= current_adminsection.email %> | <%= link_to 'Logged In [click to logout]', destroy_adminsection_session_path, :method => :delete %></p>


  
                <% else %>
                     <p>You are not Logged in. | <%= link_to 'Login', new_adminsection_session_path %></p>
  
<% end %>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>


     <div class="container">
        <div class="row">
<% flash.each do |name, msg| %>
              <div class="alert alert-<%= name == :notice ? 'success' : 'important' %>">
                <a class="close" data-dismiss="alert">&#215;</a>
                <%= content_tag :div, msg, :id => "flash_#{name}" if msg.is_a?(String) %>
              </div>
            <% end %>
             <%= yield %>

        </div>
        <br />




    </div> 

                <% end %>
          </div>
        </div>
      </div>
    </div>
